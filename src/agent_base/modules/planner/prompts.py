"""planner 的中文提示词（B2）。"""

from __future__ import annotations

PLAN_PROMPT = """你是任务规划器。把用户请求拆解为有序、可独立验证的子任务\
（最多 {max_subtasks} 条）。
只输出 JSON 数组，不要输出任何其他文字，格式：
[{{"id": 1, "goal": "一句话目标"}}, ...]
简单请求输出单条。请求：{goal}"""

REPLAN_PROMPT = """你是任务规划器。一次多步执行中有子任务失败了，请重新规划剩余工作。
总目标：{goal}

已完成的子任务（不要重复规划）：
{done}

失败的子任务及原因：
{failed}

请输出新的 JSON 数组，只包含**剩余需要执行**的子任务（最多 {max_subtasks} 条），格式：
[{{"id": 1, "goal": "一句话目标"}}, ...]
只输出 JSON 数组，不要输出任何其他文字。"""

EXECUTE_SYSTEM = """你正在执行一个多步计划中的一个子任务。
总目标：{goal}
已完成子任务及其结果：
{done}
当前子任务：{current}
围绕当前子任务工作（可用工具）；完成后用一段话总结结果，不要再调用工具。"""

SYNTH_PROMPT = """你是执行总结者。下面是一次多步执行的子任务清单与各自结果。
综合它们给用户一个连贯的最终答复；如实标注未完成/失败的子任务，不要虚构。
子任务清单：
{plan}"""
