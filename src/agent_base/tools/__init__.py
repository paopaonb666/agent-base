"""基座内置工具库（M4 起）。

``core/tools`` 是工具的**治理机制**（共享池、超时、错误归一）；本包是
**通用能力**——与业务无关、可被任意模块复用的工具实现，按子包组织：

- ``parsing``：文档解析（PDF / DOCX / TXT / Markdown → 统一文本）

工具库的依赖一律走可选 extras（如 ``[doc]``），未安装对应 extras 时
相应能力优雅降级（不注册、给出可读的安装指引），基座其余部分不受
影响。业务专属工具仍由各模块通过 ``get_tools()`` 自带。
"""
